<%= form_for(@temp) do |f| %>
  <% if @temp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp.errors.count, "error") %> prohibited this temperature from being saved:</h2>

      <ul>
      <% @temp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Farenheit %><br />
    <%= f.text_field :faren %>
  </div>

  <div class="actions">
    <%= f.submit "Convert to Celsius" %>
  </div>
<% end %>
